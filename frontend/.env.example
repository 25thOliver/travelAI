# Base URL of the Travel AI backend. Leave empty when using NGINX at same origin (Docker).
VITE_API_BASE_URL=

# Optional: API key for protected endpoints. Can also be set in the app Settings dialog.
# VITE_API_KEY=
